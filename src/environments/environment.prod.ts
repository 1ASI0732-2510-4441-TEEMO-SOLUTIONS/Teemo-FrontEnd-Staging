export const environment = {
  production: true,
  apiUrl: "https://your-production-api.com", // Cambia esto a la URL de tu API de producción
  mockBackend: false, // En producción, usar el backend real
}
